from math import *
from numpy import *

# Given is the gravitational constant:
G = 6.67384e-11

# Give value for the stellar mass, planet mass, and orbital period.
M = int(input("Stellar mass in kilograms:"))
N = int(input("Planet mass in kilograms:"))
P = int(input("Orbital period in seconds:"))

# Using the raw_input values, we find the semimajor axis given by Kepler's 3rd Law.
a = ((P^2*G*(M+N))/(4*pi^2))^(1/3)

# We find the radius of the star's wobble around the center of mass.
T = M + N
r = a * T / M
print r
